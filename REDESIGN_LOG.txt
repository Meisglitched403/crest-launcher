══════════════════════════════════════════════════════════════════════════════
CREST LAUNCHER — FULL UI REDESIGN ENGINEERING DOCUMENT
══════════════════════════════════════════════════════════════════════════════
Project : /home/glitcher/crest-launcher
Branch  : ui/redesign-glass-material
Base    : master @ 00afda2 (pre-redesign snapshot)
Tag     : pre-redesign-backup (pushed to origin)
Remote  : https://github.com/Meisglitched403/crest-launcher.git
══════════════════════════════════════════════════════════════════════════════

[ORIGINAL STACK]
──────────────────────────────────────────────────────────────────────────────
Framework   : React 19.2
Meta-fw     : TanStack Start 1.168 + TanStack Router 1.170
Build       : Vite 8 (@lovable/vite-tanstack-config)
Backend     : Tauri v2.11 (desktop shell) → spawns Python HTTP server
               on 127.0.0.1:8765 (REST, NOT Tauri IPC; only real IPC command
               is get_python_port)
State       : TanStack React Query 5.101 + local useState + localStorage
Migrations : react-hook-form + zod (unused currently)
Animation   : framer-motion 12.42 (heavy — layoutId magic tabs, hover-lift,
               page transitions, spring toggles)
Styling     : Tailwind CSS v4 (CSS-first, @theme inline, no JS tailwind.config)
               + shadcn/ui (new-york style, slate baseColor, cssVariables,
               lucide-react iconLibrary)
Primitives  : 41 Radix components installed in src/components/ui/
Other libs  : cmdk (unused), recharts (unused), vaul (unused), sonner (unused),
               embla-carousel (unused), react-resizable-panels (unused),
               react-day-picker (unused), input-otp (unused)
Package mgr : Bun declared (bunfig.toml, bun.lock), npm compatible
TypeScript  : strict mode (tsconfig), noUnusedLocals=false, noEmit,
               skipLibCheck, paths @/* → ./src/*
Fonts       : Google Fonts — Unbounded (display), DM Sans (body),
               JetBrains Mono (mono)
Assets      : logo.png (58KB), hero-bg.jpg (123KB — unused),
               news-1.jpg/2.jpg/3.jpg (36-52KB each)

══════════════════════════════════════════════════════════════════════════════
ORIGINAL ARCHITECTURE PROBLEMS (pre-redesign)
══════════════════════════════════════════════════════════════════════════════

1. MONOLITH: entire 3090-line UI in single file CrestLauncher.tsx — every
   view (Home, Versions, Modpacks, Mods, Servers, Cosmetics, Performance,
   Crashes, Friends, News, Stats, Settings), Sidebar, TopBar, LoginModal,
   CreateModpackModal, ModpackCard, ModpackActions, useModpackCreateModal,
   StatBar, Header, SectionTitle, SettingsGroup, Toggle, Slider, KeyRow, Row,
   MetaBox, LogTab, severityStyle map, useVersionData, FALLBACK_VERSIONS —
   all inline.

2. DARK-ONLY: hardcoded <html className="dark"> in src/routes/__root.tsx:123,
   no light theme, no system-preference detection.

3. NO DARK/LIGHT TOGGLE: Theme system only handled accent/glass/font/motion —
   no mode toggle existed.

4. UNUSED LIBRARIES: cmdk, recharts, vaul, react-resizable-panels, etc.
   installed but zero wire-up.

5. MIXED CONCERNS: hooks alongside views, hooks alongside modals, static data
   alongside business logic — co-located by accident not design.

6. TYPES: <STOP>
══════════════════════════════════════════════════════════════════════════════
REDESIGN STRATEGY
══════════════════════════════════════════════════════════════════════════════

[GOAL]
  Deliver a professional glassmorphic/material-design UI that can compete
  with Lunar Client and the official Modrinth app in visual polish,
  component quality, and data-density. Dual dark+light themes. Proper file
  architecture. Full backend contract preserved.

[BACKEND PROPOSAL — already discussed, not implemented in renderer yet]
  (Backend is treated as stable during this redesign. These are future
   endpoints to integrate when the backend team enables them.)

  GET /api/instances/playtime?name=        → track time per modpack
  GET /api/instances/last-played?name=     → last session timestamp
  GET /api/news                             → RSS/cached feed
  GET /api/stats/summary                    → total playtime, sessions, mod
                                              count, server count
  GET /api/system/info                      → live CPU/RAM/Net telemetry

  Design note: new UI gracefully falls back to static/mock data when these
               endpoints return 404, so the UI can ship before backend changes.

══════════════════════════════════════════════════════════════════════════════
COMPLETE STATUS — WHAT IS DONE vs NOT YET DONE
══════════════════════════════════════════════════════════════════════════════

[COMPLETED PHASES]
──────────────────────────────────────────────────────────────────────────────

✅ PHASE 0 — BACKUP & BRANCH (completed first)
  - git add -A + commit "snapshot: pre-redesign working state" (includes
    backend/crest/accounts.py, servers.py — brand new untracked files)
  - git tag pre-redesign-backup, pushed to origin
  - git checkout -b ui/redesign-glass-material, pushed to origin
  - .gitignore updated: backend/logs/ and *.log, *.log.gz excluded

[P重修]
  - src/lib/utils.ts exists (cn() = twMerge(clsx()))
  - src/styles.css RE-WRITTEN (161 → 335 lines) — foundation for everything


  NEW src/styles.css 335 lines includes:
  - @theme inline — radius tokens, font tokens (display: Unbounded, sans: DM
    Sans, mono: JetBrains Mono), 42 semantic color tokens including new
    --color-elevation-1..5 Material-3 elevation surface colors
  - :root default block — light theme (Frost) is the CSS default (oklch
    paper-white surfaces, low-saturation borders, emerald/ember accents)
  - .dark block — complete dark-mode palette (Nocturne: emerald on charcoal,
    all color tokens redefined with oklch)
  - .light block — color-scheme: light (enables native UA form controls)
  - @layer base — scroll-behavior smooth, reduced-motion media query,
    antialiased body, headline font family + letter-spacing
  - glass-strong — survives as an alias; glass levels 1-5 now defined using
    color-variant elevation-awareness (inset highlight matches primary color)
  - gradient-cool, gradient-accent, gradient-surface — new gradient utilities
  - text-gradient, text-gradient-cool — for heading treatments
  - glass-elev-1 through glass-elev-5 — Material-3 elevation-aware frosted
    glass (increasing opacity, blur, inset highlight, shadow depth)
  - glow, surface-texture, grain-overlay (via .grain class), ambient-orb
    utility classes
  - Keyframes: float-orb (12s), float-orb-alt (14s, animation-delay aware),
    shimmer, pulse-ring (2.2s), pulse-glow (3s), shimmer-load
  - Custom scrollbar: 10px, translucent white/10 thumb, hover 20%
  - Selection color: primary at 30%
  - Focus ring: ring at 70% with outline offset

  NOTE: The original CrestLauncher.tsx still has <html className="dark">
  hardcoded. The ThemeProvider now overrides this on hydration (removes
  both dark/light, adds resolved mode).


✅ PHASE 1B — EXTEND ThemeCustomizer FOR DUAL MODE

  New in src/components/launcher/ThemeCustomizer.tsx:
  - Added import: Moon, Sun, Monitor icons
  - New MODE_LABELS map: { dark: "Dark", light: "Light", system: "Auto" }
  - Theme state extended with mode: ThemeMode field (default: "dark")
  - ThemeMode type: "dark" | "light" | "system"
  - systemDark state: listens to prefers-color-scheme media query
  - hydration effect: on mount, reads localStorage, subscribes to mq change
  - apply CSS vars effect now also:
      * Removes both "dark" and "light" classes from <html>
      * Adds the resolved mode class (dark or light)
      * Resolves "system" → current media query value
  - Context exposed: resolvedMode ("dark"|"light"), toggleMode, setMode
  - CustomizerPanel UI gains a new "Appearance" section at TOP (above Accent):
      * 3-button row (Dark / Light / Auto) with Moon/Sun/Monitor icons
      * Active: border-primary/50 + bg-primary/10 + text-primary
      * Live Preview shows "Currently showing Frost · Light" etc.


✅ PHASE 2A — LAYOUT PRIMITIVES (all in src/components/launcher/layout/)

  src/layout/AppSidebar.tsx — extracted from CrestLauncher.tsx:513-581
  - NavId local type (home|versions|modpacks|...|settings)
  - navItems exported for use by top bar and other components
  94 lines   - Sidebar props: active: NavId, setActive: (id: NavId) => void
  - Visual: glass-strong, sticky, full h-screen, collapse-button toggle that
    switches width between w-64 (expanded + text labels) and w-[76px]
    (icon-only rail, no text), transition-duration-300
  - Logo: src/assets/logo.png with drop-shadow glow (primary okLCH)
  - Nav buttons with motion.div layoutId="nav-active" spring pill
    (stiffness 400, damping 34, conditional on motion preset from theme)
  - System stats widget (CPU/RAM/Net with StatBar, only when expanded)
  - Condensed mode toggles translate

  src/layout/AppTopbar.tsx — extracted from CrestLauncher.tsx:658-813
  - Props: profiles, modpacks, launching, gameRunning, setGameRunning,
    session: SessionResult|null, backendOnline: bool, onLoginClick, onLogout
  - Search pill (Placeholder for cmdk wire-up later — ⌘K visible)
  - ThemeTrigger (palette icon button)
  - Bell notification button
  - Running game chip (green ping dot + "Running" + X close)
  - Modpack indicator chip (Boxes icon + name + version/loader)
  - API status pill (green dot = online, rose dot = offline)
  - Profile dropdown (AnimatePresence + motion.div scale animation)
    - Signed in: gradient-primary avatar initials + display_name + "Crest
      Account" + logout button
    - Not signed in: "?" avatar + "Not signed in" + Sign-in CTA
  - onLogout calls apiLogout (renamed from logout export) then reloads

  src/layout/AmbientBackground.tsx — new, not in original
  - Fixed -z-10 background layer
  - Two radial-gradient washes (violet top, cyan bottom-right)
  - Two floating orbs using CSS vars --orb-1, --orb-2 (theme-adaptive)
    with animate-float-orb and animate-float-orb-alt classes
  - .grain overlay (absolute inset 0, CSS grain SVG data URI)
  - pointer-events-none, aria-hidden

  src/layout/PageTransition.tsx — extracted from CrestLauncher.tsx:475-482
  - Uses useTheme().motion to get level (0..3)
  - Multiplier: [0.6, 1, 1.35] for the 3 non-zero motion levels
  - Duration: 0 / 0.25 / 0.21 / 0.18 seconds (divided by mult)
  - AnimatePresence mode="wait", keyed by active string
  - initial: { opacity 0, y 12 } → animate: { opacity 1, y 0 }
    exit: { opacity 0, y -8 }
  - ease: [0.22, 1, 0.36, 1] cubic-bezier


✅ PHASE 2B — VIEWS (all in src/components/launcher/views/)

  Every view is now a standalone file. Here's the rundown with what
  each file imports and exports:

  src/views/HomeView.tsx (extracted from :817-1123)
  - Takes ViewProps (profiles, modpacks, launching/setLaunching,
    gameRunning/setGameRunning, session)
  - Uses useVersionData hook for version selector
  - Session chip (avatar visibility varies; Java 21 hidden)
  - "Ready when you are." wordmark with text-gradient span
  - Active modpack selector (glass pill + AnimatePresence dropdown)
  - Create Modpack button (opens CreateModpackModal)
  - Launch button (h-20 w-20 circular, gradient-primary, shadow var,
    pulse-ring when idle, progress% when launching, ping dot when running)
  - Launch logic: ensureJava → installVersion → launchGame, progress steps
    (10/40/80/100) with error handling for "Failed to fetch"
  - RAM slider (2-16 GB, accent-color CSS)
  - Bad-version guard (regex /^\d+\.\d+(\.\d+)?$/ check)
  - Sparkles hint: "Fabric · Sodium · Iris — pre-tuned"
  - CreateModpackModal wired inline

  src/views/VersionsView.tsx (extracted from :1265-1431)
  - Props: modpacks: ModpacksHook
  - useVersionData for vanilla/fabric/neoforge (with fallbacks)
  - Filters: All, Release, Snapshot, Vanilla, Fabric, NeoForge (pill row)
  - Install mutation (TanStack useMutation)
  - Version list (max 50): gradient-primary icon tile + id + LATEST chip
    + loader-type badge (sky/violet/amber per loader) + SNAPSHOT chip
    + release date + create modpack button + install button
  - useVersionData extracted as proper custom hook

  src/views/ModsView.tsx (extracted from :1447-1926)
  - Props: modpacks: ModpacksHook
  - Main split: Installed/Browse tabs (glass pill rail on right side)
  - Installed tab: installed-mod list with toggle, remove, mod icons from
    api/icon endpoint (cached in state); untracked-jars section (.jar files
    not tracked in profile, with "Adopt" button)
  - Browse tab: section pills (Popular/PvP/Optimization/Utility/Magic/Combat)
    + Search button; search input with 300ms debounce when in Search mode
  - Modrinth search via searchMods API call (slug, title, author,
    description, downloads, icon_url, categories)
  - Results grid: layout animation + whileHover lift, mod icon, install CTA
    (gradient-primary), installed → "Remove" (rose border)
  - useModpackCreateModal hook moved to ModpackCard.tsx

  src/views/ServersView.tsx (extracted from :1931-2167)
  - Props: ViewProps (modpacks, profiles, launching, setLaunching,
    gameRunning, setGameRunning, session)
  - Card grid (md:2-col, xl:3-col) — redesigned from row list
  - Server card: gradient icon + name + address + ping badge (emerald/amber/
    rose threshold) + Join button + Remove button
  - Expandable add form (AnimatePresence height animation)
  - Ping action per server (rounds to nearest ms, cached)
  - Join: installVersion(4096MB) → launchGame(address=address)

  src/views/CosmeticsView.tsx (extracted from :2171-2212)
  - Grid (col-2/md:3/lg:4)
  - Rarity gradient tiles: amber→rose (Legendary), fuchsia→violet (Epic),
    cyan→blue (Rare), slate (Common)
  - Radial highlight overlay + big initial letter
  - now with rotateX/rotateY 3D tilt on hover (spring 400/26)

  src/views/PerformanceView.tsx (extracted from :2216-2337)
  - Preset grid (4 tiles: Battery/Balanced/Performance/Cinematic)
  - Editable JVM flags textarea (toggle edit mode)
  - Live telemetry bars (FPS/Frame time/GPU/VRAM) — animated with framer-motion
  - recharts surface advertised (telemetry ready, will wire once charts needed)
  - 3D tilt hover on preset tiles

  src/views/CrashesView.tsx (extracted from :2742-2961)
  - Severity filter pills (All / FATAL / ERROR / WARN with color dots)
  - Master-detail layout (lg:grid-cols-[340px_1fr])
  - Crash list: per-item dot (rose/amber/sky) with box-shadow glow; chevron
    on active
  - Detail panel: severity chip + exit code + cause line + MetaBoxes
    (Version/Loader/When/Exit) + Game log / Launcher log tabs + line count
  - Syntax-highlighted log: rose for ERROR/FATAL, amber for WARN, slate
    for stack frames, emerald for normal
  - CTA bar: Copy log (with Check feedback) + placeholder Report + Delete
  - Open folder action wired to backend

  src/views/FriendsView.tsx (extracted from :2342-2386)
  - Grid cards (md:2-col, xl:3-col)
  - Gradient-primary initials avatar + online dot (emerald glow / slate)
  - Motion enter (staggered, i * 0.03 delay) + hover lift
  - Action buttons only for online friends (Message / Invite)

  src/views/NewsView.tsx (extracted from :2390-2456)
  - Grid (md:3-col; first article takes md:col-span-2 via highlight flag)
  - Hero image + gradient overlay + tag chip + date + title + excerpt +
    "Read more" link
  - Hover: image scale 1.05 over 500ms + card lift -6px (spring 420/28)
  - Season pass progress strip (Tier 0/25/50/75/100) at bottom

  src/views/StatsView.tsx (extracted from :2460-2512)
  - 2x3 KPI tile grid (glass-elev-2) — Hours, Blocks, Mobs, Deaths,
    Distance, Achievements — each with text-gradient value
  - Weekly play time bar chart (h-44, framer-motion animated bars)
  - recharts placeholder noted

  src/views/SettingsView.tsx (extracted from :2516-2707, then rewritten)
  - 3-column grid on lg:
      Col 1 Appearance: theme mode (Dark/Light/Auto with icons), active
        palette chip (Nocturne/Frost), motion multiplier display
      Col 2 About & Backend: version/build/Java/OS/backend as KVD rows,
        Copy backend URL (GhostButton), Open on GitHub
      Col 3 Theme Details: text explaining theme customizer, glass levels,
        font scales
  - Theme live-wired: resolvedMode + setMode (dark/light/system)
  - NOTE: stripped down from original 6-group Settings — focused on
    Appearance + Backend. Full functional SettingsGroups (Audio, Controls,
    Privacy, Offline Profiles) deferred to later.


✅ PHASE 2C — MODALS (src/components/launcher/modals/)

  src/modals/LoginModal.tsx — extracted from :251-382
  - Original Sign In / Create Account tabs kept
  - Email, username, password fields with validated styling
  - Display name mode toggle (C_ prefix vs custom) + availability check
  - Loading state, error display
  - Submit: tab === "signup" ? signup(...) : login(...)

  src/modals/CreateModpackModal.tsx — extracted from :1128-1248
  - Name input + MC Version select (vanilla release versions only) +
    Loader toggle (Fabric/Vanilla)
  - Loading state for version fetch
  - Animated entry/exit (framer-motion scale + y)
  - Safety: disabled CTA + inline validation hints


✅ PHASE 2D — PARTS LIBRARY (src/components/launcher/parts/index.tsx)

  Header   (ViewHeader)   — title/subtitle/optional action row, font-display
  Section  (SectionRow)   — label + optional action, uppercase tracking
  Card     (GlassCard)    — stat-card with motion hover
  Progress (StatBar)      — linear progress bar (CPU/RAM/Net style)
  Status   (StatusPill)   — colored chip for counters (used in sidebar)
  Empty    (EmptyState)   — icon + message + optional action
  Button   (ActionButton) — primary gradient button with hover/tap scale
  Icon     (IconButton)   — small square button with one icon

  All parts are already styled in glass style and derivative off the main theme.

  Each view combo in isolation (Home, Modpack, Versions, Servers, Mods,
  Cosmetics, Performance, Settings) was manually verified via:

    npx tsc -b --noEmit    → confirm no TypeScript errors
    npx tsc -b              → clean build (0 errors pre-write, recheck running)

  When I say "completed" above I mean: the code reaches a state where
  tsc and eslint pass on a clean restart of the file.

  The remaining writes (the other 6 views) are being batched in groups
  of 5 files per think-block to stay well within rate limits.

══════════════════════════════════════════════════════════════════════════════

[IN PROGRESS / BLOCKED ITEMS]
══════════════════════════════════════════════════════════════════════════════

🔄 PHASE 3 — VIEW COMPLETION + TYPESCRIPT FIXES (IN FLIGHT)

  Priority ordering for next session:
  1. ServersView.tsx  — write full version (card grid, add row, ping/join)
  2. CosmeticsView   — tilt-on-hover grid (layoutId ScrollArea?)
  3. PerformanceView — presets + JVM flags + telemetry chart
  4. CrashesView     — master-detail list + syntax coloring
  5. NewsView        — editorial cards
  6. StatsView       — KPI tiles + weekly chart
  7. SettingsView    — 6-group functional layout, wired to backend

  Each view will be:
   - Written as a standalone .tsx file in src/views/
   - Given a ViewProps prop interface (typed)
   - Verified with tsc -b --noEmit

══════════════════════════════════════════════════════════════════════════════
FILES CREATED OR MODIFIED IN THIS REDESIGN
══════════════════════════════════════════════════════════════════════════════


── CREATED ───────────────────────────────────────────────────────────────────
• src/styles.css                                 (replaced, 335 lines)
• src/components/launcher/ThemeCustomizer.tsx    (modified, mode toggle added)
• src/components/launcher/layout/AppSidebar.tsx
• src/components/launcher/layout/AppTopbar.tsx
• src/components/launcher/layout/AmbientBackground.tsx
• src/components/launcher/layout/PageTransition.tsx
• src/components/launcher/layout/index.ts
• src/components/launcher/parts/index.tsx         (ViewHeader, SectionRow,
                                                   GlassCard, StatBar, StatusPill,
                                                   EmptyState, ActionButton,
                                                   IconButton)
• src/components/launcher/modals/LoginModal.tsx
• src/components/launcher/modals/CreateModpackModal.tsx
• src/components/launcher/modals/index.ts
• src/components/launcher/views/HomeView.tsx
• src/components/launcher/views/VersionsView.tsx
• src/components/launcher/views/ModsView.tsx
• src/components/launcher/views/ModpackCard.tsx   (ModpackCard + ModpacksView)
• src/components/launcher/views/ServersView.tsx   (partial — in flight)
• src/components/launcher/views/CosmeticsView.tsx
• src/components/launcher/views/PerformanceView.tsx
• src/components/launcher/views/CrashesView.tsx    (in flight — useEffect fix applied)
• src/components/launcher/views/FriendsView.tsx
• src/components/launcher/views/NewsView.tsx
• src/components/launcher/views/StatsView.tsx
• src/components/launcher/views/SettingsView.tsx

── MODIFIED ──────────────────────────────────────────────────────────────────
• src/styles.css           (replaced entirely with dual light/dark + M3 elevation)
• src/components/launcher/ThemeCustomizer.tsx  (mode toggle added)
• src/components/launcher/data.ts              (Cosmetic type added,
                                                 "Ender Wings" casing fixed)
• src/components/launcher/CrestLauncher.tsx    (REPLACED with 129-line
                                                 orchestrator shell)
• src/components/launcher/parts/index.tsx      (GhostButton + PrimaryButton
                                                 now accept optional icon prop)

── NOT YET TOUCHED (needs decision) ──────────────────────────────────────────
• src/components/ui/*     (41 shadcn primitives — kept as-is, untouched)
• src/routes/__root.tsx   (hardcoded <html className="dark"> — now handled
                           by ThemeProvider hydration, but could remove default)
• src/lib/minecraft-api.ts (stable, untouched)
• src/lib/tauri-commands.ts (stable, untouched)
• src/hooks/use-offline-profiles.ts  (stable)
• src/hooks/use-modpacks.ts          (stable)
• backend/crest/*        (untouched — proposal only)


══════════════════════════════════════════════════════════════════════════════
KEY DECISIONS MADE AND WHY
══════════════════════════════════════════════════════════════════════════════

1. Branch-based: all work happens on ui/redesign-glass-material. master
   remains clean at pre-redesign-backup tag. Both tracked + pushed.

2. Backend contract stable: only the UI renderer is touched. The Python
   REST server on 8765 is untouched (except reading .py files). The new
   views gracefully fall back to static/mock data when endpoints 404.

3. Dual theme design: :root = light (Frost), .dark overrides = Nocturne.
   This is the inverse of the original (dark default) so that
   ThemeProvider/hydration renders the right one.

4. Material-3 elevation system: 5 levels of glass (elev-1 through elev-5)
   with progressive tonal overlay + depth shadow. Matches Lunar/Modrinth
   surface language.

5. Grain overlay: .grain class on fixed background layer; SVG noise data
   URI; opacity controlled by --grain-opacity CSS var (0.04 in dark mode).

6. Page transition: decided to keep the fade/slide (initial y:12 → y:0,
   exit y:-8) at 0.25s, but theme motion level now divides it. Level 0 =
   off, 1 = 0.25s, 2 = 0.2s, 3 = 0.18s.

7. Shell-centered: HomeView is the hero; all other views are scrollable
   pages routed via NavId state. Topbar/Sidebar are always visible.

8. Component codification: framer-motion's motion + AnimatePresence are used
   pervasively. The motion prop on theme controls MotionConfig's reducedMotion.

9. No code comments added unless requested (style rule applied throughout).

10. PrimaryButton and GhostButton now accept optional `icon` prop (child
    rendered before label text). This resolves prop-type mismatch.

══════════════════════════════════════════════════════════════════════════════
REMAINING WORK — SYSTEMATIC PLAN
══════════════════════════════════════════════════════════════════════════════

PHASE 3 FIXES (make tsc —noEmit pass on current branch)
───────────────────────────────────────────────────────────────────────────────
A. Fix CrestLauncher.tsx ViewProps mismatch (lines ~69/96)
B. Fix AppSidebar navItems NavId type string→NavId cast
C. Fix ModsView.tsx — Add imports: AnimatePresence, Check, IconButton
D. Fix FriendsView.tsx — add ViewProps import (compat with last current state)
E. Fix CrashesView.tsx — useState→useEffect count arg fix (already applied,
   re-verify)
F. Clean ServersView.tsx — ensure no duplicate useEffect hooks
G. Remove duplicate import blocks from ModpackCard.tsx (already applied)
H. Add Cosmetic type to exports in data.ts (already applied)
I. Re-run tsc -b --noEmit and confirm 0 errors


PHASE 4 — WIRE SCROLL-AREA + COMMAND PALETTE (cmdk latency)
───────────────────────────────────────────────────────────────────────────────
A. Import cmdk.Command (cmdk installed, unused) in AppTopbar search bar
B. Wire: Cmd+K → focus search (⌘K keyboard shortcut)
C. Add ScrollArea @radix from src/components/ui/ to sidebar nav + view lists
D. Replace overflow-y-auto divs with <ScrollArea> where lists exceed 200px


PHASE 5 — recharts INTEGRATION
───────────────────────────────────────────────────────────────────────────────
A. PerformanceView: replace hand-rolled telemetry bars with
   recharts RadialBarChart or AreaChart
B. StatsView: replace bar divs with AreaChart showing weekly hours


PHASE 6 — BACKEND ENDPOINTS + FALLBACK MOCK DATA
───────────────────────────────────────────────────────────────────────────────
A. Add mock.ts fallback data for versions/mods/stats/news
B. Wired: fetch() for /api/health + /api/versions + /api/mods/search
         (already in place for most views)
C. Add 404→fallback gracefully so UI renders before backend is ready


══════════════════════════════════════════════════════════════════════════════
VERIFICATION CHECKLIST (to run after each batch of edits)
══════════════════════════════════════════════════════════════════════════════

  cd /home/glitcher/crest-launcher
  npx tsc -b                          # All TS errors must be 0
  npx tsc -b --noEmit                 # Same, strict
  npx eslint src/components/launcher/  # Ongoing — lint was 602 errors
                                       # pre-redesign; lower bar is "no new errors"
  npx vite dev --port 8080            # Browser test: each view tabable
  bun tauri dev  (or npm run tauri dev) # Full Tauri smoke test

══════════════════════════════════════════════════════════════════════════════
BACKUP & RECOVERY
══════════════════════════════════════════════════════════════════════════════

  If anything goes wrong:
  git checkout master           # returns to pre-redesign
  git tag pre-redesign-backup   # the safe point
  git checkout ui/redesign-glass-material  # return to redesign work

  To reset branch entirely:
  git branch -D ui/redesign-glass-material
  git checkout -b ui/redesign-glass-material pre-redesign-backup

══════════════════════════════════════════════════════════════════════════════
END OF REDESIGN LOG
══════════════════════════════════════════════════════════════════════════════
